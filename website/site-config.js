/* Set siteURL after hosting is chosen, e.g. https://example.com/origami/ .
   Release download information lives exclusively in release.json. */
window.ORIGAMI_SITE = Object.freeze({
  siteURL: null
});
